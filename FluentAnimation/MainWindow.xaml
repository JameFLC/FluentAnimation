<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="FluentAnimation.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluentAnimation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:animations="using:CommunityToolkit.WinUI.Animations"
    xmlns:controls="using:FluentAnimation.Controls"
    mc:Ignorable="d"
    Title="FluentAnimation">

    <Grid Margin="0"  Background="{ThemeResource SolidBackgroundFillColorBaseBrush}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid x:Name="TitleBarElement" Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <TextBlock Margin="16,0,0,0" Text="Fluent Animation" VerticalAlignment="Center"/>
                <Rectangle Width="2" RadiusX="1" RadiusY="1" Margin="16,0,8,0" Height="32" VerticalAlignment="center" Fill="{ThemeResource DividerStrokeColorDefaultBrush}"/>
                <SelectorBar x:Name="PageSelector" SelectionChanged="PageSelector_SelectionChanged">
                    <SelectorBarItem x:Name="SelectorBarItemPage1" Text="FirstPage" IsSelected="True" />
                    <SelectorBarItem x:Name="SelectorBarItemPage2" Text="Advanced Animation" />
                </SelectorBar>
            </StackPanel>
            <Border x:Name="TitleBarGrab" Grid.Column="1"/>
        </Grid>
        <Frame x:Name="MainFrame" Grid.Row="1" />
    </Grid>
</Window>
