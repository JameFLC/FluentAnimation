<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="FluentAnimation.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluentAnimation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:animations="using:CommunityToolkit.WinUI.Animations"
    xmlns:controls="using:FluentAnimation.Controls"
    mc:Ignorable="d"
    Title="FluentAnimation">

    <Grid Margin="0"  Background="{ThemeResource SolidBackgroundFillColorBaseBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid x:Name="TitleBarElement" HorizontalAlignment="Stretch" VerticalAlignment="Center" Padding="12, 8">
            <TextBlock Text="Fluent Animation"/>
        </Grid>
        <ScrollViewer 
            Grid.Row="1">
            <GridView
                x:Name="BasicGridView"
                SelectionMode="None"
                HorizontalAlignment="Center">
                <GridView.ItemContainerStyle>
                    <Style TargetType="GridViewItem" BasedOn="{StaticResource DefaultGridViewItemStyle}">
                        <Setter Property="Margin" Value="7"/>
                    </Style>
                </GridView.ItemContainerStyle>
                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="local:InfoItemData">
                        <controls:InfoItem
                            FullName="{x:Bind Name}"
                            Address="{x:Bind Address}"
                            Job="{x:Bind Job}"/>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </ScrollViewer>
    </Grid>
</Window>
