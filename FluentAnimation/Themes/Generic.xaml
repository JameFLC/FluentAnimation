<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluentAnimation"
    xmlns:controls="using:FluentAnimation.Controls">

    <!--<Style TargetType="controls:TriangleSlider" xmlns:controls="using:FluentAnimation.Controls">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:TriangleSlider">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Controls/TriangleSlider/TrangleSlider.xaml"/>
    </ResourceDictionary.MergedDictionaries>
</ResourceDictionary>
